# Copia este archivo a ".env.local" (NO a ".env") y llena los valores.
# Usa SIEMPRE la ANON KEY de Supabase, nunca la service role key.
# Este repositorio es publico: nunca subas credenciales reales.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
